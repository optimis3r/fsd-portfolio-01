// Migrated to server/data/projects.json for Assignment 3 (Backend Integration).
// Static project data is no longer imported on the frontend and is served via GET /api/projects.
export const projectsData = [];
